# GS-xx — [Scenario Name]

> Golden E2E Scenario. Baseline list in
> `docs/ai-governance/05-E2E-QUALITY-GATES.md`; full detail for each
> scenario lives in
> `docs/system-truth-templates/17-E2E-GOLDEN-SCENARIOS.md` using this
> format.

## Business Goal
(One sentence: what business outcome this scenario proves works.)

## Actors
(Roles/users involved — e.g. Leasing Consultant, Tenant, System/Job.)

## Preconditions
(State that must exist before the scenario starts.)

## Inputs
(Concrete data used to drive the scenario.)

## Steps
1. ...
2. ...

## Expected Entity State
(Exact state of every entity touched, after each significant step or at
minimum at the end.)

## Expected Financial State
(Every money value that must be correct, with currency.)

## Permissions
(Who should succeed; who should be denied — negative case included per
Gate 7.)

## Events
(Which events/jobs should fire, and their expected effect.)

## Notifications
(Which notifications should be sent, to whom.)

## Reports
(Which Dashboard/Reports/Analytics values should reflect this scenario's
outcome, and how.)

## Reconciliation
(Cross-surface values that must match after this scenario completes.)

## Failure Variants
(At least one deliberate failure injection per Gate 5 — network loss,
concurrent conflict, invalid input — and the expected safe-failure
behavior.)
